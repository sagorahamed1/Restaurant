import 'package:flutter/cupertino(1).dart';

class SeasonalContainer extends StatelessWidget {
  const SeasonalContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
